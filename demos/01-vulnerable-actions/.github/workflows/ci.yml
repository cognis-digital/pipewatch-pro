on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: malicious/action@main
      - uses: third-party/action@v1
      - run: echo ${{ secrets.AWS_ACCESS_KEY_ID }}
    env:
      AWS_TOKEN: AKIAIOSFODNN7EXAMPLEXYZ123
